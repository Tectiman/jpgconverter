"""
jpgconverter 高级配置文件

使用方法:
    1. 复制此文件为 config.py
    2. 修改下面的配置字典
    3. 程序会自动检测并加载 config.py

注意：此文件为可选配置，不使用此文件时程序使用默认值或命令行参数
"""

# =============================================================================
# 性能选项
# =============================================================================
PERFORMANCE_OPTIONS = {
    # 最大工作线程数（建议：CPU 核心数，或 4-8）
    'max_workers': 8,

    # 批处理大小（每批文件数）
    'batch_size': 50,

    # 是否显示进度条
    'show_progress_bar': True,
}

# =============================================================================
# 转换选项
# =============================================================================
CONVERSION_OPTIONS = {
    # 默认质量 (0-100)
    'default_quality': 90,

    # 是否跳过已存在的文件
    'skip_existing': True,

    # 保留 EXIF 信息
    'keep_exif': True,
}

# =============================================================================
# 日志选项
# =============================================================================
LOGGING_OPTIONS = {
    # 详细模式（输出更多信息）
    'verbose': False,

    # 保存日志到文件
    'save_log': False,

    # 日志文件路径
    'log_file': 'jpgconverter.log',
}

# =============================================================================
# 默认值（不要修改）
# =============================================================================
DEFAULT_MAX_WORKERS = 8
DEFAULT_BATCH_SIZE = 50
DEFAULT_SHOW_PROGRESS = True
